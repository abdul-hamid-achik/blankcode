slug: working-with-models
name: Working With Models
description: >-
  The judgment that stays yours when a model writes the code: reading work you
  did not write, pinning a request down before it turns into the wrong thing,
  and building the check instead of promising to remember it.
icon: compass
color: signal
order: 10
isPublished: true
challenges:
  # Reviewing first — it is the skill every other one leans on.
  - ts-review-001
  - py-review-001
  # Then specifying, because the cheapest defect is the one never requested.
  - ts-spec-001
  - py-spec-001
  # Then tooling: stop doing the check by hand.
  - ts-tool-001
  - py-tool-001
  # Last, and hardest: the reported error is not the failure.
  - ts-review-002
