/**
 * The hosted product's auth headlines.
 *
 * Register used to say the account was local and that nothing left the
 * machine. That was a leftover from a self-host experiment and a lie on
 * blankcode.dev: progress, the review schedule, and billing live on the
 * server. These strings are the contract the pages render.
 */

export const REGISTER_HEADING = 'Create an account.'
export const REGISTER_BLURB =
  'Progress and the review schedule live with the account, so they come back on any machine.'

export const LOGIN_HEADING = 'Back to it.'
export const LOGIN_BLURB = 'Your progress and review schedule live with your account.'
